version: "3.8"
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "10000:10000"
    volumes:
      - ./app:/app
      - ./model:/app/model
    environment:
      - ENV=development
