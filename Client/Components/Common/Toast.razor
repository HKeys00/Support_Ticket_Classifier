<div class="toast-container">
    @foreach (var toast in _toasts)
    {
        <div class="toast @GetCssClass(toast.Level)">
            <h6>@toast.Title</h6>
            <span>@toast.Message</span>
        </div>
    }
</div>