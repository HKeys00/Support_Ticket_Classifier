@page "/"
@code {
    /// <summary>
    /// Gets or sets the injected <see cref="NavigationManager"/> instance.
    /// </summary>
    [Inject]
    public required NavigationManager NavigationManager { get; set; }


    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/tickets");
    }
}

<h1>HERE</h1>