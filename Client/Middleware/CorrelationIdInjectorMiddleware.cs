namespace Client.Middleware
{
    /// <summary>
    /// Middleware for generating a unique ID for each request
    /// </summary>
    public class CorrelationIdInjectorMiddleware// : IMiddleware
    {
    }
}
